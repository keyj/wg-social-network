<%= form_for(@wg) do |f| %>
  <% if @wg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wg.errors.count, "error") %> prohibited this wg from being saved:</h2>

      <ul>
      <% @wg.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :beschreibung %><br />
    <%= f.text_area :beschreibung %>
  </div>
  <div class="field">
    <%= f.label :adresse %><br />
    <%= f.text_area :adresse %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
