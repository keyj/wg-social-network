<%= form_for(@abrechnung) do |f| %>
  <% if @abrechnung.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@abrechnung.errors.count, "error") %> prohibited this abrechnung from being saved:</h2>

      <ul>
      <% @abrechnung.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :mail %><br />
    <%= f.text_area :mail %>
  </div>
  <div class="field">
    <%= f.label :bezahlt %><br />
    <%= f.check_box :bezahlt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
